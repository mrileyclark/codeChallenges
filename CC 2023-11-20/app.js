// 11.20.23 Sum Mixed Array

// function sumMix(x){
//    let result = x.map(str => {return parseInt(str, 10)});
//    return result.reduce((acc, curr) => acc + curr, 0);
// }

// console.log(sumMix([2, 3, 4, "5", "8"]))
